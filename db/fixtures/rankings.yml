strongest_ranking:
  leaderboard: strongest
  created_at: <%= (Time.now).strftime("%Y-%m-%d %H:%M:00") %>
  
indefatigable_ranking:
  leaderboard: indefatigable
  created_at: <%= (Time.now).strftime("%Y-%m-%d %H:%M:00") %>

overlords_ranking:
  leaderboard: overlords
  created_at: <%= (Time.now).strftime("%Y-%m-%d %H:%M:00") %>
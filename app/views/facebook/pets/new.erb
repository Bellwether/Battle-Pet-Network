<% content_for :page_title do %><h2>Befriend Your Pet</h2><% end %>
	
<div class="box full em">
  <p>Befriending a pet is easy, just: <ol><li>choose a breed, and</li><li>type your pet's name.</li></ol></p> 
  <p>Your pet starts with the archetypal abilities, strengths, and weaknesses of their breed, but they'll quickly grow as you customize them through further play.</p>
</div>

<div class="sidebox half right">
  <%= render_breed_details @breed %>	
</div>

<div class="box half">
  <% form_for @pet, :url => facebook_nested_url(facebook_pet_path) do |f| %>
  <%= f.hidden_field :breed_id %>
  <table class='form'>
    <tbody>
    <tr>
	  <td><%= f.label :name, 'Name' %></td>
	  <td><%= f.text_field :name %></td>
    </tr>
    <tr>
	  <td>Breed:</td>
	  <td>
  	    <%= render_breed_picker(f) %>
      </td>
    </tr>
    <tr><td colspan="2"><%= f.submit 'Befriend' %></td></tr>
    </tbody>
  </table>
  <% end %>    
</div>
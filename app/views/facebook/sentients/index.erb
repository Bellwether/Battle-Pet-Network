<% content_for :page_title do %><h2>The Sentient Threat</h2><% end %>

<div class="sidebox slim right">
  <%= render_hunts_box @hunts unless !has_pet? || @hunts.blank? %>
</div>

<div class="box">
<p class="em">
  <em>The world remains a dangerous battleground, inhabited by noble pets as well as populations of vicious predators, 
  mindless animals, mutants, feral humans, and worse. These beasts are known as 'sentients' and must be hunted down and 
  destroyed by vigilant pets like yours.</em>
</p>
<p class="em">
  Hunting sentients works just like normal battle, except that combat is resolved immediately and instead of 
  experience your pet is rewarded in Kibble for fighting the sentient menace. It's a great way to earn Kibble and 
  safeguard your community.
</p>
</div>

<div class="box">
  <h3>Active Sentients</h3>
  <% @sentients.each do |sentient| %>
  <table width="100%" class="sentients dotbox">
	<tr>
	  <td rowspan="6"><%= avatar_image(sentient,'large') %></td>
	  <th colspan="2">	
        <%= "<span class='right hunt-button'>#{link_to_hunt(sentient)}</span>" if has_pet? %>
		<h6><%= facebook_link_to sentient.name, facebook_sentient_path(sentient) %></h6>
	  </th>
	</tr>
	<tr>
	  <td width="150">Health: <label><%= sentient.health %></label></td>
	  <td rowspan="5" class="em"><%= sentient.description %></td>
	</tr>
	<tr><td>Endurance: <label><%= sentient.endurance %></label></td></tr>
	<tr><td>Power: <strong><%= sentient.power %></strong></td></tr>
	<tr><td>Award: <strong><%= sentient.kibble %> Kibble</strong></td></tr>
	<tr><td>Population: <strong><%= "#{sentient.population} / #{sentient.population_cap}" %></strong></td></tr>
  </table>
  <% end %>
  <%= will_paginate @sentients %>
</div>